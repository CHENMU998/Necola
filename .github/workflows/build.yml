name: Build Necola

on:
  push:
    branches: [ main, master, fix ]
  workflow_dispatch:

env:
  VCPKG_ROOT: ${{ github.workspace }}/vcpkg

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 45

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup xmake
      uses: xmake-io/github-action-setup-xmake@v1
      with:
        xmake-version: latest

    - name: Setup MSVC
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: x86

    - name: Install vcpkg (classic mode)
      run: |
        git clone --depth=1 https://github.com/microsoft/vcpkg.git ${{ env.VCPKG_ROOT }}
        cd ${{ env.VCPKG_ROOT }}
        .\bootstrap-vcpkg.bat
        echo "VCPKG_ROOT=${{ env.VCPKG_ROOT }}" >> $env:GITHUB_ENV

    - name: Cache vcpkg packages
      uses: actions/cache@v4
      with:
        path: |
          ${{ env.VCPKG_ROOT }}/installed
          ${{ env.VCPKG_ROOT }}/packages
          ${{ env.VCPKG_ROOT }}/downloads
        key: ${{ runner.os }}-vcpkg-x86-${{ hashFiles('xmake.lua') }}
        restore-keys: |
          ${{ runner.os }}-vcpkg-x86-

    - name: Configure xmake
      run: |
        xmake f -m release -a x86 -y -v
      env:
        VCPKG_ROOT: ${{ env.VCPKG_ROOT }}

    - name: Build project
      run: |
        xmake -y -v
      env:
        VCPKG_ROOT: ${{ env.VCPKG_ROOT }}

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: necola-build-x86
        path: |
          build/windows/x86/release/necola.dll
          build/windows/x86/release/left4dead2_necola.exe
          kpatch.ini
